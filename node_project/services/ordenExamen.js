const db = require("./db");

module.exports = {
    addSolicitudExamen: async (solicitud) => {

        // let query = [
        //     "INSERT INTO Reporte (idPaciente, nit, fechaEmision, idSucursal) VALUES (?, ?, ?, ?)",
        //     "INSERT INTO RESULTADO_CAMPO (idReporte, nombreExamen, nombreCampo, precio, estado"
        // ];

        // let message = "Hubo un error al crear la solicitud de examen.";
        // if (result.affectedRows)
        //     message = "La solicitud de examen se ingreso con exito.";

        // return { message };
    },
};
